# ========================================
# 环境变量配置示例
# ========================================
# 复制此文件为 .env.local 或 .env.production 并填入实际值
# 注意: 请勿将包含敏感信息的 .env 文件提交到代码仓库

# ========================================
# 数据库配置
# ========================================
# PostgreSQL 连接字符串
# 格式: postgresql://[user]:[password]@[host]:[port]/[database]?[params]
# 参数说明:
#   - sslmode=require: 强制使用 SSL 连接 (生产环境推荐)
#   - max=10: 最大连接数 (默认 10)
#   - idle_timeout=20: 空闲连接超时时间，单位秒 (默认 20)
#   - connect_timeout=10: 连接超时时间，单位秒 (默认 10)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require&max=10&idle_timeout=20&connect_timeout=10

# ========================================
# 认证配置
# ========================================
# JWT 密钥 (请使用强随机字符串，至少 32 字符)
# 生成命令: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT 过期时间 (可选，默认 7d)
JWT_EXPIRES_IN=7d

# ========================================
# 应用配置
# ========================================
# 服务端口
PORT=3000

# 运行环境 (development | production)
NODE_ENV=production

# 应用域名 (用于 CORS 等配置)
APP_DOMAIN=vitacross.cn

# ========================================
# 阿里云配置 (可选)
# ========================================
# OSS 配置 (用于文件存储)
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=
# OSS_BUCKET=
# OSS_REGION=oss-cn-hangzhou
# OSS_ENDPOINT=

# ========================================
# AWS S3 配置 (可选)
# ========================================
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_BUCKET=
# AWS_ENDPOINT=

# ========================================
# 日志配置 (可选)
# ========================================
# 日志级别 (error | warn | info | debug)
LOG_LEVEL=info

# ========================================
# 邮件配置 (可选)
# ========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@vitacross.cn

# ========================================
# 其他配置
# ========================================
# API 速率限制 (每秒请求数)
RATE_LIMIT_PER_SECOND=10

# 文件上传最大大小 (MB)
MAX_UPLOAD_SIZE=20

# 启用详细日志
DEBUG=false
